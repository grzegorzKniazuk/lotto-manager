export interface FormGroupErrorTooltipConfig {
    formGroupErrorName: string; // nazwa blędu na który nasłuchujemy w FromGroup
    addictedFormGroupControlsNames: string[]; // kontrolki których błąd dotyczy
}
